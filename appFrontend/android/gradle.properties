# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
# Increase Gradle HTTP timeouts to reduce transient download failures on large artifacts.
systemProp.org.gradle.internal.http.connectionTimeout=180000
systemProp.org.gradle.internal.http.socketTimeout=180000

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Native libs per CPU. Do NOT restrict to arm64 only: most Android Studio emulators on Windows
# are x86_64 and will crash on launch (SoLoader / UnsatisfiedLinkError) without x86/x86_64 .so files.
# Play Store AAB splits by ABI automatically; larger local APK is expected.
# To build a single ABI locally: ./gradlew :app:assembleDebug -PreactNativeArchitectures=x86_64
#
# reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
# The new architecture triggers extra codegen/CMake work that is failing on
# Windows in this project. Disable it for stable local release builds.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
# On Windows, if release build fails with "Failed to open file: ... index.android.bundle", try hermesEnabled=false as a workaround.
hermesEnabled=false

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=false
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=false
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# Whether the app is configured to use edge-to-edge via the app config or `react-native-edge-to-edge` plugin
expo.edgeToEdgeEnabled=false
NODE_BINARY=node

# Release signing — keystore file lives under android/app/
# Passwords: use env vars MYAPP_UPLOAD_STORE_PASSWORD / MYAPP_UPLOAD_KEY_PASSWORD
# or create android/keystore.properties (see keystore.properties.example). Do not commit passwords.
MYAPP_UPLOAD_STORE_FILE=upload-keystore.jks
MYAPP_UPLOAD_KEY_ALIAS=upload

# Compose compiler 1.5.14 (Expo SDK 52 stack) rejects Kotlin 1.9.25 by default.
# Suppress this strict check to keep Android release builds moving on this toolchain.
androidx.compose.compiler.suppressKotlinVersionCompatibilityCheck=1.9.25